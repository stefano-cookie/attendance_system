# Database Configuration
DB_USER=stebbi
DB_PASSWORD=stebbi
DB_NAME=attendance_system
DB_HOST=localhost
DB_PORT=5432

# JWT Configuration
JWT_SECRET=your-secret-key-here

# SMTP Email Configuration (per email presenze)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=attendance@unicantemir.it
SMTP_PASSWORD=your-app-password-here

# Node Environment
NODE_ENV=development